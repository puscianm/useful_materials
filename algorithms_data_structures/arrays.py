"""
Arrays

Definition
----------
collection of elements of the same memory size.
Address of first element is called first address, foundation address
or base address.

Each address of element can be calculated as B + k * c

where B is beginning address, k is index of element and c is byte size of datatype in array.

operations
----------
* access (end) - O(n) if unknown end element
* access (beginning) - O(1)
* access (middle) - O(n)
* peek by index - O(n)
"""
